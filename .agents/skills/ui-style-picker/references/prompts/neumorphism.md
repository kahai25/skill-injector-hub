# Neumorphism — component restyle prompt

Paste this verbatim, replacing the placeholder with the component code. It restyles the aesthetic layer only; layout, structure, labels, and behavior stay untouched.

```text
Apply the **Neumorphism** design style to the component provided below.

Do NOT change:
• layout
• HTML/JSX structure
• component hierarchy
• text labels
• functionality

Modify ONLY the aesthetic layer:
colors, shadows, gradients, radii, lighting, surfaces, and typography tone.

Follow these exact Neumorphism rules:

BASE
• Background color: #ECF0F3 (or an extremely close shade)
• No borders, no outlines, no dividers
• All elements appear molded from a single matte surface

LIGHTING
• Light source ALWAYS top-left
• Use these exact shadow values for raised elements:
  - Outer shadow: 6px 6px 12px rgba(0,0,0,0.12)
  - Highlight: -6px -6px 12px rgba(255,255,255,1)
• For inset/pressed elements, use:
  - Inner shadow: inset 6px 6px 12px rgba(0,0,0,0.10),
                  inset -6px -6px 12px rgba(255,255,255,0.7)

SHAPE
• Rounded corners required (minimum radius 12px)
• No sharp edges anywhere
• Buttons, inputs, toggles, sliders = soft, pill-like shapes

COLOR
• Use ONLY monochrome or near-monochrome variations of the base color
• If an accent color is required, it must be a VERY soft pastel with 10–20% saturation
• Never use strong or vivid colors

TYPOGRAPHY
• Light or regular weight
• Text color: #7A7E80 or similar low-contrast grey
• No pure black text

STRICT FORBIDDEN RULES
• DO NOT use borders
• DO NOT use bright or saturated colors
• DO NOT add glass, transparency, or blur
• DO NOT use strong shadows or high contrast
• DO NOT change the light direction
• DO NOT flatten the component (it must look raised or inset)

Output must always match classic Neumorphism: soft, tactile, molded surfaces with dual shadows.

---------------------------------
COMPONENT TO STYLE:
[Paste your component code here]
---------------------------------
```
